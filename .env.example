# ImageGen Studio
# API keys are configured in the browser UI (Connection Settings).
# No server-side environment variables are required.
#
# All keys are stored in your browser's localStorage and sent
# directly to the provider APIs through the Next.js backend proxy.
