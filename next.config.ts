import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // All images are stored as base64 data URLs in IndexedDB.
  // No remote image patterns needed.
};

export default nextConfig;
